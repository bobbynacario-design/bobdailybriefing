firebase-debug.log*
firestore-debug.log*
ui-debug.log*
.firebase/

# generated into the deployable Functions source from the browser-shared core
functions/command-center-core.js

# radar — local-only secrets and deps (never committed)
radar/node_modules/
radar/serviceAccountKey.json
radar/.env
radar/refresh.log
radar/refresh-ph.log

# miro — local-only secrets and deps (never committed; reuses radar's secrets)
miro/node_modules/
miro/serviceAccountKey.json
miro/.env
miro/refresh.log

# news — local-only deps and key (never committed; reuses radar's service account)
news/node_modules/
news/serviceAccountKey.json
news/.env
news/refresh.log

# sports - local-only secrets and deps
sports/node_modules/
sports/serviceAccountKey.json
sports/.env
sports/refresh.log
sports/refresh-*.log
sports/run-history.json

# local re-tagger backup of original briefing JSON (never commit user content)
radar/retag-backup.json
