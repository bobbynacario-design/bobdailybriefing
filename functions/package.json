{
  "name": "bobdailybriefing-functions",
  "description": "Backend OpenAI generator for Bob's Daily Intelligence Briefing",
  "scripts": {
    "pretest": "npm run sync:shared-core",
    "test": "node --test *.test.js",
    "precheck": "npm run sync:shared-core",
    "check": "node --check index.js && node --check delivery-core.js && node --check research-result.js && node --check webhook-event.js && node --check briefing-evidence.js && node --check briefing-context.js && node --check market-facts.js && node --check briefing-prompt-core.js",
    "predeploy": "npm run sync:shared-core",
    "deploy": "firebase deploy --only functions:bobdailybriefing --project pokerhq-a67e4",
    "sync:shared-core": "node sync-shared-core.js"
  },
  "engines": {
    "node": "24"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "openai": "^7.4.0"
  },
  "private": true
}
